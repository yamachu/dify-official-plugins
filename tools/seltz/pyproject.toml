[project]
name = "dify-seltz"
version = "0.0.1"
description = "Seltz Web Knowledge API plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.5.0",
    "seltz>=0.2.0",
]

[dependency-groups]
dev = []
